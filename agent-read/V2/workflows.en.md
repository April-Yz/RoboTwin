# Workflows

Phase 1 sequentially plans and executes pregrasp, grasp, lift, and place, corrects the place offset after close, and saves only physically successful trajectories. Phase 2 reconstructs the same action sequence, validates the v2 pickle, replays it, records data, and generates instructions.
